# Notas:
Este es mi primer programa de Node

```
    Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Base de la tabla                            [number] [required]
  -h, --hasta    El limite hasta donde se imprime la tabla   [number] [required]
  -l, --listar   Imprime en consola la tabla          [boolean] [default: false]

```